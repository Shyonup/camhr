package com.camhr.job.config;

/**
 * <pre>
 * 旧的项目，camhr项目src目录下，有个conf.properties文件，里面配置的很多全局常量，
 * 这里，把上面这个文件配置的部分常量给抽取过来
 *
 * Created by Allen on 2018/9/19.
 * </pre>
 */
public class JobApplyConfiguration {

  /**
   * employer can view resume before apply + ? days.
   */
  public static final int JOB_APPLY_VIEW_CV_DAYS = 30;

}
